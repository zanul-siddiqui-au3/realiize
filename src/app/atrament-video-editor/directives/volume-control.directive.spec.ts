import { VolumeControlDirective } from './volume-control.directive';

describe('VolumeControlDirective', () => {
  it('should create an instance', () => {
    const directive = new VolumeControlDirective();
    expect(directive).toBeTruthy();
  });
});
